/*

list --> Allows duplicates, ordered item on bases of Indexing

set --> Doesn't duplicates, ordered item on Random Indexing on items

Converting list to set -->  toset(list_name)

toset(["e","a", "b", "c","a"])
toset([
  "a",
  "b",
  "c",
  "e",
])


*/
